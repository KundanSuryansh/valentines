# Google Sheets API
# Single Google Sheet with two tabs:
# - "card" tab: Column A = JSON per row (one card per row)
# - "home" tab: Column A = key, Column B = value (homepage text config)
# Enable Sheets API, create API key, share sheet: "Anyone with the link can view"

VITE_GOOGLE_SHEET_ID=
VITE_GOOGLE_API_KEY=
