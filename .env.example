# Google Sheets API
# Cards sheet: Column A = JSON per row (one card per row)
# Homepage sheet: Column A = key, Column B = value (homepage text config)
# Enable Sheets API, create API key, share both sheets: "Anyone with the link can view"

VITE_GOOGLE_SHEET_ID=
VITE_GOOGLE_HOMEPAGE_SHEET_ID=
VITE_GOOGLE_API_KEY=
